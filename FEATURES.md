# Morrowind MCP Server Features

## Prompts

| Name | Title | Description |
|---|---|---|
| `mw-instruct` | | Instruct the agent on Morrowind, the MCP, and the autonomy level. |
| `mw-loar` | | Tell me about loar of this. |
| `mw-navigate` | | Find directions to your destination. |
| `mw-role` | | Role-play the character in Morrowind. |
| `mw-todo` | | Tell me what to do next. |
| `mw-translate` | | Translate it! |
| `mw-walkthrough` | | Give me some tips on how to beat the game. |

## Resources

| Name | URI | Title | Description |
|---|---|---|---|
| `memory/actor/index.json` | morrowind://memory/actor/index.json | Actor Index | Index of observed Morrowind actors. |
| `memory/index.json` | morrowind://memory/index.json | Memory Index | Root index of Morrowind memory resources. |
| `memory/player.json` | morrowind://memory/player.json | Player Summary | Summary state of the player character. |
| `memory/player/equipment.json` | morrowind://memory/player/equipment.json | Player Equipment Items | Equipped items of the player. |
| `memory/player/inventory.json` | morrowind://memory/player/inventory.json | Player Inventory Items | Carried inventory items of the player. |
| `memory/player/journal.json` | morrowind://memory/player/journal.json | Journal Entries | Journal entries of the player. |
| `memory/player/progression.json` | morrowind://memory/player/progression.json | Player Progression | Level, attributes, and skills of the player character. |
| `memory/player/quests.json` | morrowind://memory/player/quests.json | Quest Entries | Active and completed quests of the player. |
| `memory/player/spellbook.json` | morrowind://memory/player/spellbook.json | Player Spellbook | Known spells, powers, and abilities of the player. |
| `memory/player/vitals.json` | morrowind://memory/player/vitals.json | Player Vitals | Health, magicka, fatigue, and active effects of the player character. |
| `memory/unattributed/dialogue.json` | morrowind://memory/unattributed/dialogue.json | Unattributed Dialogue Notes | Dialogue notes observed while no specific actor was active. |
| `memory/unattributed/notifications.json` | morrowind://memory/unattributed/notifications.json | Unattributed Notification Notes | Recent notifications observed while no actor dialogue or specific context was active. |
| `memory/unattributed/observations.json` | morrowind://memory/unattributed/observations.json | Unattributed Observations | Recent observations recorded while no specific actor or target context was active. |
| `memory/world/visited_cells.json` | morrowind://memory/world/visited_cells.json | Visited Cells | Index of cells visited during this session. |

## Tools

| Name | Title | Description | Input | Output | Annotations |
|---|---|---|---|---|---|
| `mw-capabilities-fetch` | | Fetch general conditions for published tools. We recommend using this tool to check then. | <ul><li><code>tool_name</code> (string, optional): Optional published tool name to filter by.</li></ul> | <ul><li><code>tools</code> (array)</li></ul> | <ul><li>readOnlyHint</li></ul> |
| `mw-debug-action` | | Perform a debug command. | <ul><li><code>action</code> (string, required): Debug command to perform.</li></ul> | | |
| `mw-dialogue-action` | | Select a topic, choice, service, or persuasion action returned by mw-dialogue-fetch, or close MenuDialog with the bye shortcut. | <ul><li><code>kind</code> (string, required): Kind returned by mw-dialogue-fetch, or bye to close MenuDialog.</li><li><code>text</code> (string, optional): Exact displayed text returned by mw-dialogue-fetch. Not used by bye.</li><li><code>menu_path</code> (string, optional): Optional exact path returned by mw-dialogue-fetch. It detects a changed or ambiguous dialogue menu.</li></ul> | <ul><li><code>selected</code> (object)</li></ul> | |
| `mw-dialogue-fetch` | | Fetch the current dialogue menu conversation, notifications, choices, topics, services, persuasion action, and unclassified actions. This includes only text displayed in the current conversation; use memory resources for past conversations. Unclassified actions cannot be selected by mw-dialogue-action. | | <ul><li><code>actor</code> (object)</li><li><code>dialogues</code> (array)</li><li><code>notifications</code> (array)</li><li><code>choices</code> (array)</li><li><code>topics</code> (array)</li><li><code>services</code> (array)</li><li><code>persuasion</code> (array)</li><li><code>unknown_actions</code> (array)</li></ul> | <ul><li>readOnlyHint</li></ul> |
| `mw-inventory-action` | | Run one diagnostic inventory click-to-place probe. This tool is available only in development debug mode. | <ul><li><code>action</code> (string, required): The single inventory operation to attempt through the displayed inventory menus.</li><li><code>source_menu_path</code> (string, optional): Path of an inventory tile returned by mw-menu-fetch. Required except for transfer_all and offer.</li></ul> | | <ul><li>destructiveHint</li></ul> |
| `mw-inventory-fetch` | | Fetch current player's inventory. or instead read the resource: morrowind://memory/player/inventory.json | | <ul><li><code>inventory</code> (array)</li></ul> | <ul><li>readOnlyHint</li></ul> |
| `mw-menu-action` | | Action to a non-root menu element selected by its path. | <ul><li><code>menu_path</code> (string, required): Action to perform using a <code>path</code> returned by mw-menu-fetch; paths use raw MWSE child indexes, not serialized array positions.</li><li><code>action</code> (string, required): Action to perform on the menu.</li><li><code>text</code> (string, optional): (Optional) Text to input if action is <code>textInput</code>.</li></ul> | | |
| `mw-menu-fetch` | | Fetch current menu hierarchy. <code>menu</code> is user interface such as inventory. <code>help</code> is overlay such as tooltips. some menus have <code>widget</code> or <code>actionable</code> properties to indicate what kind of action can be performed on this menu. | <ul><li><code>menu_id</code> (number, optional): Fetch a non-root hierarchy of menu by ID (key name is <code>id</code>). If not specified, all menus will be returned. One of <code>menu_id</code> or <code>menu_name</code> should be specified.</li><li><code>menu_name</code> (string, optional): Fetch a non-root hierarchy of menu by name (key name is <code>name</code>). If not specified, all menus will be returned. One of <code>menu_id</code> or <code>menu_name</code> should be specified.</li><li><code>output_mode</code> (string, optional): Return the menu tree, flat actionable elements, or both. The default is both.</li></ul> | <ul><li><code>menu</code> (object)</li><li><code>help</code> (object)</li><li><code>actions</code> (array)</li><li><code>cursor_tile</code> (object)</li></ul> | <ul><li>readOnlyHint</li></ul> |
| `mw-player-action` | | Perform direct player input such as movement keys, activation, jumping, sneaking, combat preparation, or other short manual actions. Use this for interaction, immediate input, or fine movement adjustment. For intentional travel toward a known world destination, mw-route-navigate may be more suitable. For managing inventory items such as equip/unequip items, barter items with a merchant, transfer items between container and drop item, mw-inventory-action may be more suitable. | <ul><li><code>action</code> (string, required): Action to perform on the player character.</li><li><code>how</code> (string, required): How to perform the action. Tap is a single press, push is a press and hold, hammer is a rapid repeat.</li><li><code>seconds</code> (number, optional): Time in seconds to hold the action. Only used for push and hammer.</li></ul> | | |
| `mw-player-fetch` | | Fetch current player state. | <ul><li><code>detail_level</code> (string, optional): Serialization detail for game objects. The default is standard.</li></ul> | <ul><li><code>player</code> (object)</li><li><code>menu_mode</code> (boolean): Whether the active game is in menu mode.</li></ul> | <ul><li>readOnlyHint</li></ul> |
| `mw-player-look` | | Direct the player view at an active reference or to absolute world-space angles. Any active player navigation is cancelled before the view is applied. | <ul><li><code>mode</code> (string, required): target looks at the nearest active reference with target_id. angles uses absolute yaw and pitch in degrees.</li><li><code>target_id</code> (string, optional): Required for target mode. Matches active-cell reference base IDs; nearest matching reference is used.</li><li><code>yaw</code> (number, optional): Required for angles mode. Absolute compass heading in degrees: 0 is north and 90 is east.</li><li><code>pitch</code> (number, optional): Required for angles mode. Absolute vertical angle in degrees; positive values look upward.</li></ul> | <ul><li><code>yaw</code> (number): Applied absolute yaw in degrees.</li><li><code>pitch</code> (number): Applied absolute pitch in degrees.</li><li><code>target_id</code> (string): Resolved target ID when mode is target.</li><li><code>target_point_kind</code> (string): Method used to select the target look point.</li><li><code>navigation_cancelled</code> (boolean): Whether an active player navigation route was cancelled before looking.</li></ul> | |
| `mw-reference-fetch` | | Fetch references near the player by default, or from all active cells when requested. In minimal and standard results, distance is reported from the player as both Morrowind game units and meters; reference positions remain Morrowind game-space coordinates in game units. | <ul><li><code>category</code> (array, optional): Filter references by category. If not specified, all categories will be returned. activators is can be activated and interacted by the player. actors is characters and NPCs. statics is not moved by self objects such as items.</li><li><code>id</code> (string, optional): Filter references by ID. If not specified, all references will be returned.</li><li><code>detail_level</code> (string, optional): Serialization detail. The default is minimal for lists and full when filtering by reference ID.</li><li><code>scope</code> (string, optional): Reference cell scope. nearby includes the player's current cell and loaded neighbours near exterior cell boundaries. active includes all active cells.</li></ul> | <ul><li><code>activators</code> (array)</li><li><code>actors</code> (array)</li><li><code>statics</code> (array)</li><li><code>serialization</code> (object)</li></ul> | <ul><li>readOnlyHint</li></ul> |
| `mw-route-fetch` | | Fetch travel transitions that are reachable on foot from the player's current position, and whether an optional destination is walkable. Travel transitions currently come from doors leading to another cell. This tool does not move the player. | <ul><li><code>position_x</code> (number, optional): Optional destination X coordinate in world space, in Morrowind game units.</li><li><code>position_y</code> (number, optional): Optional destination Y coordinate in world space, in Morrowind game units.</li><li><code>position_z</code> (number, optional): Optional destination Z coordinate in world space, in Morrowind game units.</li><li><code>cell_id</code> (string, optional): Optional cell containing the destination.</li></ul> | <ul><li><code>destination_walkable</code> (boolean): Whether the optional destination has a known walk-only route.</li><li><code>travel_nodes</code> (array)</li></ul> | <ul><li>readOnlyHint</li></ul> |
| `mw-route-navigate` | | Navigate the player character through the game world toward an intentional destination, such as an observed NPC, reference, location, or world-space coordinate. Navigation returns route node and waypoint counts; verify arrival with player, reference, or world state. When navigation does not start, movement_started is false and travel_nodes contains compact reachable transitions with reference_id, kind, position, destination cell_id, and walk_distance; use mw-route-fetch for details. Use cancel_navigation to stop an active route. | <ul><li><code>action</code> (string, required): Method to navigate the player character or cancel active navigation.</li><li><code>position_x</code> (number, optional): Destination X coordinate in world space, in Morrowind game units.</li><li><code>position_y</code> (number, optional): Destination Y coordinate in world space, in Morrowind game units.</li><li><code>position_z</code> (number, optional): Destination Z coordinate in world space, in Morrowind game units.</li><li><code>cell_id</code> (string, optional): Optional cell containing the destination.</li></ul> | <ul><li><code>route_node_count</code> (number): Number of pathgrid nodes in the started route.</li><li><code>waypoint_count</code> (number): Number of waypoints in the started route.</li><li><code>reason</code> (string): Why navigation did not start.</li><li><code>movement_started</code> (boolean): False when no route was started.</li><li><code>travel_nodes</code> (array)</li></ul> | |
| `mw-screenshot-save` | | Save a screenshot of the current game state to a file. The screenshot will be saved to the resources | <ul><li><code>capture_with_ui</code> (boolean, optional): The screenshot will include the user interface.</li><li><code>file_name</code> (string, optional): Screenshot file name without extension. If not specified, a timestamp will be used.</li></ul> | | |
| `mw-spell-fetch` | | Fetch the player's spells, powers, and castable magic items. | | <ul><li><code>spells</code> (array)</li><li><code>powers</code> (array)</li><li><code>magic_items</code> (array)</li></ul> | <ul><li>readOnlyHint</li></ul> |
| `mw-spell-select` | | Select a spell from the player's spells, powers or magic items in the magic menu. | <ul><li><code>category</code> (string, required): Category returned by mw-spell-fetch.</li><li><code>id</code> (string, optional): Exact ID returned by mw-spell-fetch.</li><li><code>name</code> (string, optional): Exact name returned by mw-spell-fetch.</li><li><code>menu_path</code> (string, optional): Exact MenuMagic element path returned by mw-menu-fetch.</li></ul> | <ul><li><code>selected</code> (object)</li></ul> | |
| `mw-target-fetch` | | Fetch current target state. This is the object that the player is currently looking at or cursor is currently pointing at. | <ul><li><code>detail_level</code> (string, optional): Serialization detail for game objects. The default is standard.</li></ul> | <ul><li><code>playerTarget</code> (object)</li><li><code>lookingAt</code> (object)</li><li><code>serialization</code> (object)</li></ul> | <ul><li>readOnlyHint</li></ul> |
| `mw-world-fetch` | | Fetch the world state. | | <ul><li><code>world</code> (object)</li></ul> | <ul><li>readOnlyHint</li></ul> |
