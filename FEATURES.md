# Morrowind MCP Server Features

## Prompts

| Name | Title | Description |
|---|---|---|
| `mw-instruct` | | Instruct the agent on Morrowind, the MCP, and the autonomy level. |
| `mw-loar` | | Tell me about loar of this. |
| `mw-navigate` | | Find directions to your destination. |
| `mw-role` | | Role-play the character in Morrowind. |
| `mw-todo` | | Tell me what to do next. |
| `mw-translate` | | Translate it! |
| `mw-walkthrough` | | Give me some tips on how to beat the game. |

## Resources

| Name | URI | Title | Description |
|---|---|---|---|
| `memory/index.json` | morrowind://memory/index.json | Memory Index | Root index of Morrowind memory resources. |
| `memory/player/equipment.json` | morrowind://memory/player/equipment.json | Player Equipment Memory | Memory snapshot of the current player's equipped items. |
| `memory/player/index.json` | morrowind://memory/player/index.json | Player Memory | Memory entity for the current player. |
| `memory/player/inventory.json` | morrowind://memory/player/inventory.json | Player Inventory Memory | Memory snapshot of the current player's inventory. |
| `memory/player/journal.json` | morrowind://memory/player/journal.json | Player Journal Memory | Memory collection of current player journal entries. |
| `memory/player/progression.json` | morrowind://memory/player/progression.json | Player Progression Memory | Current player level, attributes, and skills. |
| `memory/player/quests.json` | morrowind://memory/player/quests.json | Player Quest Memory | Memory collection of current player quest states. |
| `memory/player/spellbook.json` | morrowind://memory/player/spellbook.json | Player Spellbook Memory | Current player spells and powers. |
| `memory/player/visited-cells.json` | morrowind://memory/player/visited-cells.json | Player Visited Places Memory | Places the player has entered, represented by TES3 cells. |
| `memory/player/vitals.json` | morrowind://memory/player/vitals.json | Player Vitals Memory | Current player health, magicka, fatigue, and life state. |
| `memory/unattributed/dialogue.json` | morrowind://memory/unattributed/dialogue.json | Unattributed Dialogue Memory | Dialogue text observed without a resolved actor. |
| `memory/unattributed/index.json` | morrowind://memory/unattributed/index.json | Unattributed Memory | Observations that do not have a stable domain subject. |
| `memory/unattributed/notifications.json` | morrowind://memory/unattributed/notifications.json | Notification Memory | Transient notification text observed without a stable domain subject. |

## Tools

| Name | Title | Description | Input | Output | Annotations |
|---|---|---|---|---|---|
| `mw-capabilities-fetch` | | Fetch general conditions for published tools. We recommend using this tool to check then. | <ul><li><code>tool_name</code> (string, optional): Optional published tool name to filter by.</li></ul> | <ul><li><code>tools</code> (array)</li></ul> | <ul><li><code>readOnlyHint</code>: true</li></ul> |
| `mw-debug-action` | | Perform a debug command. | <ul><li><code>action</code> (string, required): Debug command to perform.</li></ul> | | |
| `mw-dialogue-action` | | Select... use mw-dialogue-fetch before this tool | | | |
| `mw-dialogue-fetch` | | Fetch the content of the current conversation with the actor you're talking to from the dialogue menu. Only the content of this conversation can be fetched. To access past conversations, read from the memory resource. | | <ul><li><code>actor</code> (object)</li><li><code>dialogues</code> (array)</li><li><code>topics</code> (array)</li></ul> | <ul><li><code>readOnlyHint</code>: true</li></ul> |
| `mw-inventory-action` | | Run one diagnostic inventory click-to-place probe. This tool is available only in development debug mode. | <ul><li><code>action</code> (string, required): The single inventory operation to attempt through the displayed inventory menus.</li><li><code>source_menu_path</code> (string, optional): Path of an inventory tile returned by mw-menu-fetch. Required except for transfer_all and offer.</li></ul> | | <ul><li><code>destructiveHint</code>: true</li></ul> |
| `mw-inventory-fetch` | | Fetch current player's inventory. or instead read the resource: morrowind://memory/player/inventory.json | | <ul><li><code>inventory</code> (array)</li></ul> | <ul><li><code>readOnlyHint</code>: true</li></ul> |
| `mw-menu-action` | | Action to a non-root menu element selected by its path. | <ul><li><code>menu_path</code> (string, required): Action to perform using a <code>path</code> returned by mw-menu-fetch; paths use raw MWSE child indexes, not serialized array positions.</li><li><code>action</code> (string, required): Action to perform on the menu.</li><li><code>text</code> (string, optional): (Optional) Text to input if action is <code>textInput</code>.</li></ul> | | |
| `mw-menu-fetch` | | Fetch current menu hierarchy. <code>menu</code> is user interface such as inventory. <code>help</code> is overlay such as tooltips. some menus have <code>widget</code> or <code>actionable</code> properties to indicate what kind of action can be performed on this menu. | <ul><li><code>menu_id</code> (number, optional): Fetch a non-root hierarchy of menu by ID (key name is <code>id</code>). If not specified, all menus will be returned. One of <code>menu_id</code> or <code>menu_name</code> should be specified.</li><li><code>menu_name</code> (string, optional): Fetch a non-root hierarchy of menu by name (key name is <code>name</code>). If not specified, all menus will be returned. One of <code>menu_id</code> or <code>menu_name</code> should be specified.</li><li><code>output_mode</code> (string, optional): Return the menu tree, flat actionable elements, or both. The default is both.</li></ul> | <ul><li><code>menu</code> (object)</li><li><code>help</code> (object)</li><li><code>actions</code> (array)</li><li><code>cursor_tile</code> (object)</li></ul> | <ul><li><code>readOnlyHint</code>: true</li></ul> |
| `mw-player-action` | | Perform direct player input such as movement keys, activation, jumping, sneaking, combat preparation, or other short manual actions. Use this for interaction, immediate input, or fine movement adjustment. For intentional travel toward a known world destination, mw-player-navigate may be more suitable.For managing inventory items such as equip/unequip items, barter items with a merchant, transfer items between container and drop item, mw-inventory-action may be more suitable. | <ul><li><code>action</code> (string, required): Action to perform on the player character.</li><li><code>how</code> (string, required): How to perform the action. Tap is a single press, push is a press and hold, hammer is a rapid repeat.</li><li><code>seconds</code> (number, optional): Time in seconds to hold the action. Only used for push and hammer.</li></ul> | | |
| `mw-player-fetch` | | Fetch current player state. | <ul><li><code>detail_level</code> (string, optional): Serialization detail for game objects. The default is standard.</li></ul> | <ul><li><code>player</code> (object)</li></ul> | <ul><li><code>readOnlyHint</code>: true</li></ul> |
| `mw-player-look` | | Direct the player view at an active reference or to absolute world-space angles. Any active player navigation is cancelled before the view is applied. | <ul><li><code>mode</code> (string, required): target looks at the nearest active reference with target_id. angles uses absolute yaw and pitch in degrees.</li><li><code>target_id</code> (string, optional): Required for target mode. Matches active-cell reference base IDs; nearest matching reference is used.</li><li><code>yaw</code> (number, optional): Required for angles mode. Absolute compass heading in degrees: 0 is north and 90 is east.</li><li><code>pitch</code> (number, optional): Required for angles mode. Absolute vertical angle in degrees; positive values look upward.</li></ul> | <ul><li><code>yaw</code> (number)</li><li><code>pitch</code> (number)</li><li><code>target_id</code> (string)</li><li><code>target_point_kind</code> (string)</li><li><code>navigation_cancelled</code> (boolean)</li></ul> | |
| `mw-player-navigate` | | Navigate the player character through the game world toward an intentional destination, such as an observed NPC, door, reference, location, or world-space coordinate. This is useful for travel when a reachable destination is known. Navigation returns route node and waypoint counts; verify arrival with player, reference, or world state. Use cancel_navigation to stop an active route. | <ul><li><code>action</code> (string, required): Method to navigate the player character or cancel active navigation.</li><li><code>position_x</code> (number, optional): Destination X coordinate in world space, in Morrowind game units.</li><li><code>position_y</code> (number, optional): Destination Y coordinate in world space, in Morrowind game units.</li><li><code>position_z</code> (number, optional): Destination Z coordinate in world space, in Morrowind game units.</li><li><code>cell_id</code> (string, optional): Optional cell containing the destination.</li></ul> | <ul><li><code>route_node_count</code> (number)</li><li><code>waypoint_count</code> (number)</li></ul> | |
| `mw-reference-fetch` | | Fetch references near the player by default, or from all active cells when requested. In minimal and standard results, distance is reported from the player as both Morrowind game units and meters; reference positions remain Morrowind game-space coordinates in game units. | <ul><li><code>category</code> (array, optional): Filter references by category. If not specified, all categories will be returned. activators is can be activated and interacted by the player. actors is characters and NPCs. statics is not moved by self objects such as items.</li><li><code>id</code> (string, optional): Filter references by ID. If not specified, all references will be returned.</li><li><code>detail_level</code> (string, optional): Serialization detail. The default is minimal for lists and full when filtering by reference ID.</li><li><code>scope</code> (string, optional): Reference cell scope. nearby includes the player's current cell and loaded neighbours near exterior cell boundaries. active includes all active cells.</li></ul> | <ul><li><code>activators</code> (array)</li><li><code>actors</code> (array)</li><li><code>statics</code> (array)</li><li><code>serialization</code> (object)</li></ul> | <ul><li><code>readOnlyHint</code>: true</li></ul> |
| `mw-screenshot-save` | | Save a screenshot of the current game state to a file. The screenshot will be saved to the resources | <ul><li><code>capture_with_ui</code> (boolean, optional): The screenshot will include the user interface.</li><li><code>file_name</code> (string, optional): Screenshot file name without extension. If not specified, a timestamp will be used.</li></ul> | | |
| `mw-spell-fetch` | | Fetch  | | <ul><li><code>spells</code> (array)</li></ul> | <ul><li><code>readOnlyHint</code>: true</li></ul> |
| `mw-spell-select` | | Select a spell from the player's spells, powers or magic items in the magic menu. | <ul><li><code>name</code> (string, optional): Name of the spell to select.</li></ul> | | |
| `mw-target-fetch` | | Fetch current target state. This is the object that the player is currently looking at or cursor is currently pointing at. | <ul><li><code>detail_level</code> (string, optional): Serialization detail for game objects. The default is standard.</li></ul> | <ul><li><code>playerTarget</code> (object)</li><li><code>lookingAt</code> (object)</li><li><code>serialization</code> (object)</li></ul> | <ul><li><code>readOnlyHint</code>: true</li></ul> |
| `mw-world-fetch` | | Fetch the world state. | | <ul><li><code>world</code> (object)</li></ul> | <ul><li><code>readOnlyHint</code>: true</li></ul> |
